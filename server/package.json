{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index",
    "dev": "cross-env NODE_ENV=development && nodemon --legacy-watch",
    "test:db:up": "docker compose -f ../docker-compose.test.yml down -v && docker compose -f ../docker-compose.test.yml up -d && sleep 3",
    "test:db:down": "docker compose -f ../docker-compose.test.yml down -v",
    "test:run": "cross-env NODE_ENV=test jest --detectOpenHandles --runInBand",
    "test": "npm run test:db:up && cross-env TEST_DB_URL=postgresql://testuser:testpassword@localhost:54322/testdb npm run test:run && npm run test:db:down",
    "test:watch": "npm run test:db:up && cross-env TEST_DB_URL=postgresql://testuser:testpassword@localhost:54322/testdb jest --watch --runInBand",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "google-auth-library": "^8.7.0",
    "googleapis": "^112.0.0",
    "helmet": "^4.4.1",
    "jsonwebtoken": "^8.5.1",
    "moment": "^2.29.4",
    "morgan": "^1.10.0",
    "nodemailer": "^6.8.0",
    "pg": "^8.8.0",
    "pino": "^6.11.3",
    "stripe": "^8.138.0",
    "swagger-ui-express": "^4.6.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "cross-env": "^7.0.3",
    "eslint": "^7.32.0",
    "eslint-plugin-babel": "^5.3.1",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^29.5.0",
    "nodemon": "^2.0.20",
    "pino-pretty": "^4.8.0",
    "prettier": "^2.8.1",
    "supertest": "^6.3.3"
  }
}
