apiVersion: v1
kind: Service
metadata:
  name: frontend-svc 
  labels:
    app.kubernetes.io/name: ecommerce-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP 
  selector:
    app.kubernetes.io/name: ecommerce-frontend
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80         
      targetPort: 8080 
      protocol: TCP